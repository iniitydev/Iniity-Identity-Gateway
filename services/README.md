# Backend Services

This directory contains all backend microservices for the Identity Federation Analyzer project. Each subdirectory is a self-contained service with its own dependencies, configuration, and source code.

## Services

-   `/identity-service`: Handles core business logic related to identity management, user provisioning, and federation configuration.
